<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mybatis.dao.one-to-many.TutorMapper">
    <!--使用内嵌结果ResultMap实现一对多映射-->
    <resultMap id="courseResult" type="course">
        <id property="courseId" column="course_id"/>
        <result property="name" column="name"/>
        <result column="description" property="description"/>
        <result column="start_date" property="startDate"/>
        <result column="end_date" property="endDate"/>
    </resultMap>

    <resultMap id="addressResult" type="address">
        <id property="addrId" column="addr_id"/>
        <result property="street" column="street"/>
        <result property="city" column="city"/>
        <result property="state" column="state"/>
        <result property="zip" column="zip"/>
        <result property="country" column="country"/>
    </resultMap>

    <resultMap id="tutorResult" type="tutor">
        <id property="tutorId" column="tutor_id"/>
        <result column="tutor_name" property="name"/>
        <result column="email" property="email"/>
        <association property="address" column="addr_id" select="findAddressByTutor"/>
        <collection property="courses" column="tutor_id" select="findCoursesByTutor"/>
    </resultMap>

    <select id="findTutorById" parameterType="int" resultMap="tutorResult">
        SELECT
          tutor_id,
          name AS tutor_name,
          email,
          addr_id
        FROM tutors
        WHERE tutor_id = #{id}
    </select>

    <select id="findCoursesByTutor" parameterType="int" resultMap="courseResult">
        SELECT * from courses WHERE tutor_id = #{tutorId}
    </select>

    <select id="findAddressByTutor" parameterType="int" resultMap="addressResult">
        select *
        from address
        WHERE id = #{addrId}
    </select>
</mapper>