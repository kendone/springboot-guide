<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mybatis.dao.one-to-one.StudentMapper">
    <!--使用嵌套ResultMap和association标签定义一对一(一个学生拥有一个唯一的住址)关联模型-->
    <resultMap id="addressMap" type="address">
        <id property="id" column="id"/>
        <result property="street" column="street"/>
        <result property="city" column="city"/>
        <result property="state" column="state"/>
        <result property="zip" column="zip"/>
        <result property="country" column="country"/>
    </resultMap>
    <!-- 嵌套结果ResultMap方式-->
    <resultMap id="studentAddressMap" type="Student">
        <id property="studId" column="stud_id"/>
        <result property="name" column="name"/>
        <result property="email" column="email"/>
        <!--使用association指定student实体拥有一个address(属性)实体-->
        <association property="address" resultMap="addressMap"/>
    </resultMap>
    <!--使用SQL关联查询返回结果集-->
    <select id="selectStudentAddress" parameterType="int" resultMap="studentAddressMap">
        SELECT
            id,
            name,
            email,
            a.id,
            street,
            city,
            state,
            zip,
            country
        FROM student s
            LEFT JOIN address a
                ON s.address_id = a.id
        WHERE id = #{studId};
    </select>
</mapper>